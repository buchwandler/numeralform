# v0.1.0 benchmark gate evidence

Status: **planned/unreleased**. No tag or publication was performed.

## Completed gates

- `python -m pytest benchmarks/tests`: 74 passed.
- `python -m benchmarks.download num2words --replace`: pinned oracle verified at commit `07814cb114157f582c40a00119c2e9faba8dcee2`.
- `python -m benchmarks.randomized --cases 10000 --seed 20260910 --target canonical --fail-on-unaccepted --fail-on-coverage-gap`: passed; 7,122 exact matches, 2,878 accepted variants, 0 mismatches, and no coverage gap.

## Explicitly unresolved external gates

- `python -m benchmarks.run num2words`: executed against the pinned oracle, but failed with 4,312 mismatches out of 7,207 cases. The report is retained in the ignored local path `benchmarks/data/results/num2words.txt`. This remains an unresolved compatibility gate.
- `python -m benchmarks.randomized --cases 10000 --seed 20260910 --target compat --fail-on-diff --fail-on-coverage-gap`: did not complete within the 15-minute execution window. A constrained 100-case `en` cardinal run completed with 8 mismatches, so the full compatibility gate remains unresolved.
- `python -m benchmarks.run cldr`: blocked because the pinned maintainer environment dependency PyICU is unavailable. This is an explicit environment gate, not a silently skipped check.

Core product tests, coverage, Ruff, mypy, packaging, artifact metadata, wheel isolation, and releaseledger evidence checks are recorded separately in Taskledger and releaseledger. These unresolved external gates intentionally keep v0.1.0 planned.
